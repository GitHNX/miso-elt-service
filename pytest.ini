[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
log_cli = true
log_cli_level = WARNING

markers =
    integration: marks tests that require a live PostgreSQL instance (deselect with -m 'not integration')
    slow: marks tests that are slow
